import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sergesvalov.spaceinvasion',
  appName: 'Space Invasion',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
